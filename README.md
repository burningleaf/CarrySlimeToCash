# 带呆呆史莱姆越障换钱

一个平台跳跃游戏，加一套配套的关卡数值验证工具链。单人开发，Unity 2022.3 + URP 2D。

做这个不是为了做游戏，是为了解决一个具体问题：**"这关设计得好不好"到底是主观判断，还是能算出来的数字。**

<!-- 待补：录完视频、截完图后启用
**[60 秒演示](https://www.bilibili.com/video/BVxxxxxx)**
![游戏画面](docs/images/gameplay.png) -->

## 项目状态

先说清楚做到哪了：

| 模块 | 完成度 | 说明 |
|---|---|---|
| 关卡数据管线 + 数据编辑器 | 100% | 场景 ↔ JSON 双向转换，往返校验通过 |
| 关卡求解器（玩家层） | 65% | 15 项自检全过；石头下限、史莱姆层未做 |
| 机关联动验收 | 完成 | 59 项断言，命令行无头运行 |
| Level0 教程关 | 70% | 15 块路牌暂用占位文字，象形图待做 |
| Level1 | 75% | 内容齐了，待人工走查 |
| Level2 | 35% | 地形和物件放好了，还没放机关 |
| Level3 | 0% | — |
| UI | 85% | 主菜单、选关、暂停、结算、HUD 都在 |
| 美术 | 15% | 全是纯色占位块 |
| 音效 / 动画 / 粒子 | 0% | 一个都没有 |

**表现层基本没做，这是故意的。** 我想先确认核心乐趣不靠美术资源也成立，所以先把力气花在能验证设计的工具上，而不是画面。

## 核心模型：时间 = 金钱

史莱姆会随时间掉血，**血条同时就是售价条**：1 点血 = 5 元，也就是每秒掉 5 元。

金币面值 10 元，所以：

```
10 元 ÷ 5 元/秒 = 2.0 秒
```

**一枚金币，正好够买史莱姆 2 秒的耐心。** 绕路 1.3 秒能到 → 值；绕路 2.6 秒 → 不值。

这样"跳还是不跳"就从一个手感问题，变成一道能算的题。

设计意图是让史莱姆从直觉上的"拖累"变成玩法的核心：所以它绑定了关卡收益，时间惩罚直接打在它的血条上。玩家看着血条掉，就能直观判断这枚金币值不值得绕。

### 为什么是 5 元/秒

两个约束：

1. **数值区间**：每秒时间价值要落在 3~5 元/秒。太小的话时间压力是假的，金币的绕路预算会大到失去意义。
2. **整数化**：关卡的 `parTime` 只要求取 10 的倍数，所有对外可见的数就都是整数：

```
1 秒 = 5 元 = 1 点血 = 半枚金币的绕路预算
```

没有小数，玩家能心算，不用查表。

### 由模型得出的两个数

- **冲刺**：走最短路径，**一枚金币都不捡**。跑得最快，血条剩得最多，但只有售价
- **全收集**：绕路把金币捡了。时间长、血条掉得多，但金币多

**收益差 = （全收集总分 ÷ 冲刺总分）− 1，分母是冲刺总分。**

Level1 实测：冲刺 **425 元**，全收集 **506 元** → 收益差 **+19.1%**。

它衡量的是：**绕路收集相对于完全不绕路，能多赚多少。**

分母取"冲刺"而不是"关卡资源总额"，是因为满格血量和全部金币不可能同时拿到，那样算出来的数没有决策含义；而"冲刺"是一个真实玩家能达成的结果，两条路都是可行策略，比较才有意义。

拆开看，这个比值其实是：

```
收益差 = (全收集金币 − 因绕路损失的血量价值) ÷ 冲刺总分
```

因为全收集的售价更低（时间长 → 掉血多），但金币更多。所以收益差为正，意味着**绕路拿到的金币大于掉血损失的钱**。

这就是关卡数值的体检指标。

## 验收判据：15% ~ 35%

这个区间是我拍的，但依据要说清：

| 收益差 | 玩家实际体验 |
|---|---|
| < 10% | 收集要素是装饰品，没动机绕路 |
| 15% ~ 35% | 会纠结，但不被强迫 |
| > 40% | 不捡就过不去，变成猜谜 |

**要说清楚：15%~35% 是设计目标，不是实测结论。** 它的作用是把"这关好不好"从主观判断变成可验收的数字，仅此而已。这个区间还没有经过真实玩家测试校准。

## 数据

| 关卡 | 最短时间 | 直线下界 | 冲刺 | 全收集 | 收益差 |
|---|---|---|---|---|---|
| Level0 | 20.5s | 19.4s | 697 元 | 834 元 | +19.7% |
| Level1 | 35.0s | 30.3s | 425 元 | 506 元 | +19.1% |
| Level2 | 16.6s | 13.4s | 417 元 | 498 元 | +19.4% |

三关都落在 15%~35% 区间内。

- 最短时间：玩家跑完这关的最短耗时，求解器算的
- 直线下界：水平距离 ÷ 移速。任何走法都不可能比这更快，用来防"求解器算错"
- Level1 还算出有 11 枚金币值得捡，其余绕路不划算

另外，受伤扣血按关卡经济规模自动缩放：撞一下扣 15% 血量，在总收益 458 元的关卡里等于 69 元，在 333 元的关卡里等于 50 元。相对惩罚始终一致，不用逐关调伤害数值。

## 工具链

四个 Unity Editor 工具，入口都在 `Tools/呆呆史莱姆/` 菜单下。

**关卡求解器** — `Editor/LevelSolver.cs`

把关卡抽象成"站点图"。节点不是格子，是每段可站立面上的若干站点：段两端、出生点、终点、每枚金币的位置，以及与所有邻段的最近点。段内相邻站点连"走"边，按距离计费；跨段连"跳"边。

用 Dijkstra 正反向各跑一次，几毫秒出结果。输出最短时间、哪些金币值得捡、有没有逃课路线。

**求解器自检** — 菜单 `◇◇-1`

15 项。其中 6 个是答案能手算的迷你关卡，用来把结果钉死。

**机关联动验收** — `Editor/MechTest.cs`

不开 Play 模式，用反射驱动真实脚本跑机关联动，59 项断言。命令行运行，不用打开 Unity：

```
Unity.exe -batchmode -quit -projectPath <工程路径> -executeMethod MechTest.BatchMechTest
```

**数据管线** — `Editor/LevelDataWindow.cs`

场景导出成 JSON、JSON 重建回场景、还有一步往返一致性检查（导出 → 重建 → 再导出）。

关卡数据因此变成纯文本，能 diff、能 grep、能进 Git。往返检查是用来证明管线没坏的，不靠人肉比对。

## 踩过的坑

求解器的 15 项自检抓出 4 个 bug，共同特征是**结果看起来合理，但其实是错的**。

| 现象 | 真正原因 |
|---|---|
| 一条 92 格长的地面被当成瞬移，Level1 报出 7.0 秒（直线下界 24.0 秒） | 建图时把整段当一个节点，段内走路没计费 |
| 报出"先爬柱子再跳进井里"这种荒谬路线 | 掉落按"跳"计价时用了 continue 短路，漏连了一条更便宜的边 |
| 某处"可以直接落到地面"的边根本不成立 | 下落途中半空有平台，人会落在上面 |
| 移动平台把玩家带到 x43（本该停在 x33） | 路点挂在平台的子物体上，位置跟着缩放漂移 |

### 站点图改了三次

**第一代**：一个可站立面 = 一个节点。最直觉的做法，结果是段内走路免费——一条 92 格长的地面等于瞬移，Level1 报出 7.0 秒。

**第二代**：每段取两端两个节点，段内走路收费。但起跳被绑死在端点上，竖井里嵌在坑底的台阶（dx=0 的垂直跳）永远连不上，求解器报"不可达"。

**第三代**：每段上取若干站点。段内相邻站点连"走"边，跨段连"跳"边，并且"与所有其他段的最近点"也要成为站点。前两代的问题都解决了，还顺带能算出金币的绕路代价。另外加新平台时站点会自动出现在对的位置，不用手写死。

### 无头测试的两个妥协

测试跑在编辑模式下，有两个问题必须说明：

- **`Awake` / `Start` 不会被调用**（Unity 只在运行时调）。测试里显式补调一次。不补的话，移动平台的 `_active` 停在 `false`（平台活着但不动），敌人的 `_startX` 停在 `0`（每帧判定该转身，原地抽搐）——看起来像机关坏了，其实是测试没搭对。
- **`Time.time` 不走、物理回调不派发**。由测试自己做重叠查询，把真实 Collider 交给产品的处理函数；与时间有关的行为（路点等待、松手容差）显式模拟，并在报告里注明。

## 怎么跑

用 Unity 2022.3.62f3c1 打开工程根目录（第一次打开会重建 `Library/`，要几分钟），打开 `Assets/_Project/Scenes/Level1.unity`，然后：

| 步骤 | 操作 | 预期 |
|---|---|---|
| 1 | 菜单 `Tools/呆呆史莱姆/◇◇ 求解当前关卡` | 35.0s / 下界 30.3s / 收益差 +19.1% / 11 枚金币值得捡 |
| 2 | 菜单 `Tools/呆呆史莱姆/◇◇-1 求解器自检` | 15 项全过 |
| 3 | 菜单 `Tools/呆呆史莱姆/▷ 建机关测试关 LevelMech` | 生成机关测试关 |
| 4 | 命令行跑 `MechTest.BatchMechTest` | 机关联动 59 项全过 |
| 5 | 菜单 `Tools/呆呆史莱姆/⓪-3 往返一致性检查` | 导出 → 重建 → 再导出，数据一致 |

## 目录

```
Assets/_Project/
├── Editor/          全部工具（39 个菜单入口）
│   ├── LevelSolver.cs       关卡求解器 + 15 项自检
│   ├── MechTest.cs          机关联动验收（59 项）
│   └── LevelDataWindow.cs   数据管线
├── Scripts/         运行时逻辑
├── Levels/          关卡数据 JSON（Level0/1/2）
├── Scenes/          关卡场景
└── Docs/            设计文档
```

---

罗松岩｜lsyxlb897@163.com
